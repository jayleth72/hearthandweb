/** @type {import('tailwindcss').Config} */
module.exports = {
  // ...existing config...
  plugins: [
    // ...existing plugins...
    // Note: line-clamp is built into Tailwind CSS v3+, no plugin needed
  ],
}
